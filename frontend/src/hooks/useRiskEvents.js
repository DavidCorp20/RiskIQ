import { useCallback,useEffect,useState } from 'react'
import { listRiskEvents,listRiskActions } from '../api'
export function useRiskEvents(datasetId=''){const[events,setEvents]=useState([]),[actions,setActions]=useState([]),[loading,setLoading]=useState(false),[error,setError]=useState('');const refresh=useCallback(async()=>{if(!datasetId){setEvents([]);setActions([]);return}setLoading(true);try{const[a,b]=await Promise.all([listRiskEvents(datasetId),listRiskActions()]);setEvents(a?.events||[]);setActions(b?.actions||[])}catch(e){setError(e?.message||'Unable to load risk events')}finally{setLoading(false)}},[datasetId]);useEffect(()=>{refresh()},[refresh]);const actionByEvent=actions.reduce((m,a)=>{(m[a.event_id]??=[]).push(a);return m},{});return{events,actions,actionByEvent,loading,error,refresh}}
