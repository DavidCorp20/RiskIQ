import { ResponsiveContainer, BarChart, Bar, XAxis, YAxis, Tooltip, CartesianGrid } from 'recharts'
export default function RatingDistribution({data=[]}){return <section className="portfolio-panel"><div className="portfolio-panel-heading"><div><span>RATING DISTRIBUTION</span><h3>Exposure por Rating</h3></div><small>Distribución calculada sobre el corte</small></div>{data.length?<div className="portfolio-chart"><ResponsiveContainer width="100%" height={280}><BarChart data={data} margin={{top:8,right:8,left:0,bottom:8}}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e7ebf0"/><XAxis dataKey="rating" tick={{fontSize:11}} axisLine={false} tickLine={false}/><YAxis tick={{fontSize:10}} axisLine={false} tickLine={false}/><Tooltip formatter={(v)=>['$ '+Number(v||0).toLocaleString(),'Exposure']}/><Bar dataKey="exposure" fill="#1f4f8f" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div>:<div className="portfolio-empty">No hay Rating disponible; se utiliza DPD como clasificación alternativa.</div>}</section>}